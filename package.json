{
  "name": "not-osu",
  "version": "1.0.0",
  "description": "not-osu game with frontend and backend",
  "scripts": {
    "install:all": "cd api && npm install && cd ../frontend && npm install",
    "build": "cd frontend && npm run build",
    "dev:backend": "cd api && npx ts-node-dev -T server.ts",
    "dev:frontend": "cd frontend && npm run dev"
  },
  "engines": {
    "node": ">=18.x"
  }
}

